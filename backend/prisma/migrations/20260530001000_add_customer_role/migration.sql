BEGIN TRY

BEGIN TRAN;

ALTER TABLE [dbo].[StudentProfile]
ADD [customerRole] NVARCHAR(20) NOT NULL CONSTRAINT [StudentProfile_customerRole_df] DEFAULT 'STUDENT';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
